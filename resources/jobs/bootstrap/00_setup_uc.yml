resources:
  jobs:
    bootstrap_00_setup_uc:
      name: "DocuBricks — Bootstrap 00: Unity Catalog [${bundle.target}]"
      tasks:
        - task_key: run
          python_file:
            path: src/bootstrap/setup_unity_catalog.py
          new_cluster:
            spark_version: "15.4.x-scala2.12"
            node_type_id: "Standard_DS3_v2"
            num_workers: 0
            spark_conf:
              catalog_name: ${var.catalog_name}
              secret_scope: ${var.secret_scope}
      max_concurrent_runs: 1
      timeout_seconds: 600
